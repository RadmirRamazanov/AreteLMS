services:
  web:
    build: .
    container_name: arete-lms-web
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - FLASK_DEBUG=0
    volumes:
      - arete_lms_instance:/app/instance

volumes:
  arete_lms_instance:


